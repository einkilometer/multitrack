# multitrack

A browser-based audio player for microphone pattern analysis.  
Features solo/mute logic, playhead timer, and visual indicators.


Hosted via GitHub Pages.

https://einkilometer.github.io/multitrack/multitrack_3.html

.


.

.

.

.

.

.

.

.

.

.

.

.

.



# How to delete file in gitHub
  
git config --global user.name "YourUserName"
  
git config --global user.email "YourE-Mail"
  
mkdir github
  
cd github
  
git init
  
git config --global init.defaultBranch main
  
git branch -m main
  
git clone https://github.com/einkilometer/multitrack.git
  
ls
  
cd multitrack
  
ls
  
ls -l
  
rm '07-7  t.bone EM 9600 in Cardioid mode.mp3'
  
rm '08-8  t.bone EM 9600 in Tele mode.mp3'
  
git add -u
  
git commit -m "Remove files"
  
git push orgin main
  
git remote -v
  
git push origin main
  
git config --global credential.helper cache
  
git config --global credential.helper store
  
git push origin main


# Erstelle ein neues Token unter GitHub Token Settings

https://github.com/settings/tokens

Evtl. global classic

Sehr umfangreich Einstellungen möglich.

Wähle Berechtigungen (read, write, delete)

Den Token beim nächsten Push-Versuch als „Passwort“ eingeben

Right Mouse click, Str+v, Win+V funktioniert  unter WSL nicht für die Passworteingabe



git push origin main
  
git remote set-url origin https://github.com/einkilometer/multitrack.git
  
git push -u origin main
  
history


